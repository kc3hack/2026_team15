name: Auto Assign

on:
  pull_request:
    types: [opened]

permissions:
  pull-requests: write

jobs:
  auto_assign:
    runs-on: ubuntu-latest
    steps:
      - uses: toshimaru/auto-author-assign@v3.0.1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
